pipeline {
	agent {
		label {
		label 'built-in'
		customWorkspace "/mnt/ASSIGNMENT/22Q1"
		}
	}
	stages {
		stage ('copy for data') {
			steps {
				sh "chmod -R 777 /mnt"
				sh "cp /mnt/ASSIGNMENT/22Q1/index.html /var/www/html/"
			}
		}
	}
}
